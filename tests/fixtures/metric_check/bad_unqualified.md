# Deliberately bad fixture — DO NOT COPY

This file exists so the metric-qualifier check can be proven to fail. Every line below is
a violation the checker must catch. It is excluded from repository scans via
`EXCLUDED_PATH_PARTS` in `tools/check_metric_qualifiers.py`.

The classifier reaches 90% accuracy.

| Metric | Score |
|---|---|
| Accuracy | ~90% |

Precision was 0.9013 and recall was 0.89.

Confidence: 85%
